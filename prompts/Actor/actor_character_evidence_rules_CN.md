# 演员层角色状态证据规则

演员层不判断角色卡创建、覆盖、数值属性变化、状态永久性、成长、同伴卡变化或头像 metadata。只有当场景简报允许角色状态证据，并且正文有明确可见证据时，才写入角色状态证据候选。

- 正文可以叙述可见的伤势、疲劳、士气、恐惧、羁绊、装备负担和同伴状态。
- 不得把正文后果换算成精确 HP、SAN、属性、等级、标签、成长百分比或永久状态。
- 不确定状态应保持为不确定观察。
- 最终是否接受、合并、拒绝并写入角色卡，由后续校验决定。

