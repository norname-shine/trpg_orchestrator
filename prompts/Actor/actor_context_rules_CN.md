# 演员层上下文规则

只使用本回合注入的可见记忆、当前剧情位置和场景简报。

- 缺失的可选上下文视为不可用，不视为可以自行补全。
- 隐藏真相、未来规划、渲染细节、本地文件和管理备注不得进入正文或写回。
- 如果有用细节没有提供，就在场景中保留不确定性，不要把它补成事实。
- 需要额外能力时，只通过 `capability_escalation_request` 请求，并延后到后续回合处理。

