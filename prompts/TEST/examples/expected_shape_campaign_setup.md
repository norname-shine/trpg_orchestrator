# Expected Shape: Campaign Setup

- Output is strict JSON only.
- `public_think` has 4-8 safe public waiting notes.
- `analysis.source` is `deepseek_v4_campaign_setup`.
- `campaign_direction.background_direction` is concrete and not `默认`.
- `story_blueprint_patch.chapters` exists.
- Medium story has about 6 chapters and about 24 total nodes.
- Every node has `node_id`, `goal`, `next_nodes`, and `beat_checklist`.
- `character_card_patch.profile.background` is concrete and not `AI定`.
- Companion is concrete because `companion_enabled=true`.
- Safety lines are preserved or interpreted without deletion.
